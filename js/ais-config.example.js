// TEMPLATE (safe to commit). Copy this file to  js/ais-config.local.js  and put
// your real AISStream.io key in the copy. The .local.js copy is git-ignored.
//
// Get a free key at https://aisstream.io (sign in, create an API key).
// Without a key, the map still shows the basemap — just no live vessels.
window.AIS_API_KEY = "YOUR_AISSTREAM_API_KEY";
