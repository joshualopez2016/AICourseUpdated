// AISStream.io API key for the live vessel layer on the map background.
//
// This file IS committed and deployed on purpose: AISStream runs as a browser
// websocket, so the key must be present in client-side code for the live ships
// to work on the public site — it is inherently visible in the browser either
// way. This is a FREE key (no billing) and rate-limited, so treat it as
// low-sensitivity; regenerate it anytime from your AISStream account if abused.
window.AIS_API_KEY = "cf33797ca28c3f264e30049edbeb836aaa11f106";
